import {CapacitorConfig} from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.timokluser.mycrypto',
  appName: 'MyCrypto',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
